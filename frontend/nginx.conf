server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all routes serve index.html
    # /api routing is handled by the external nginx proxy, not here
    location / {
        try_files $uri $uri/ /index.html;
    }
}